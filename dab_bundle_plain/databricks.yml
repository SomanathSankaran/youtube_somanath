# This is a Databricks asset bundle definition for dab_bundle_plain.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_bundle_plain
  uuid: 20d972df-f149-421d-ac56-8fb92938530a

artifacts:
  python_artifact:
    type: whl
    build: python -m uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    presets:
      name_prefix: '${workspace.current_user.short_name}'
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-8451ffdb-4ccd.cloud.databricks.com
    permissions:
      - user_name: somanathsankaran@gmail.com
        level: CAN_MANAGE

  prod:
    presets:
      name_prefix: 'prod'
    mode: production
    workspace:
      host: https://dbc-8451ffdb-4ccd.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/somanathsankaran@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/somanathsankaran@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: somanathsankaran@gmail.com
        level: CAN_MANAGE
