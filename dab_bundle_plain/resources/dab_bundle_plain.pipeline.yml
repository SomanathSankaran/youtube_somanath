# The main pipeline for dab_bundle_plain
resources:
  pipelines:
    dab_bundle_plain_pipeline:
      name: dab_bundle_plain_pipeline
      catalog: workspace
      schema: dab_bundle_plain_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
