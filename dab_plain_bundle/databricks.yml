# This is a Databricks asset bundle definition for dab_plain_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_plain_bundle
  uuid: 76ce6ca1-b60b-4490-a5fc-282bb6627cd5

run_as:
  user_name: somanathsankaran@gmail.com
include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    #mode: development
    default: true
    workspace:
      host: https://dbc-8451ffdb-4ccd.cloud.databricks.com
      #root_path: /Workspace/Users/somanathsankaran@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: somanathsankaran@gmail.com
        level: CAN_MANAGE
    presets: 
      name_prefix: "dev_"
      pipelines_development: true 
      trigger_pause_status: UNPAUSED #
      tags:
        env: development
        project: youtube_somanath
    variables:
      test_variable:
        description: "A test variable for dev"
        default: "default_value"


      

  prod:
    mode: production
    workspace:
      host: https://dbc-8451ffdb-4ccd.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/somanathsankaran@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/somanathsankaran@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: somanathsankaran@gmail.com
        level: CAN_VIEW
    presets: 
      name_prefix: "prod_" 
      trigger_pause_status: UNPAUSED #
      tags:
        env: production
        project: youtube_somanath
    variables:
      test_variable:
        description: "A test variable for prod"